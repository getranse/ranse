/** Max facts extracted per resolved ticket by the memory extractor. */
export const MAX_FACTS_PER_TICKET = 6;

/** Max chars of ticket transcript fed into the memory-extraction prompt. */
export const MAX_TRANSCRIPT_CHARS = 12_000;
